spring:
  application:
    name: configserver
  profiles:
    active: native

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  cloud:
    config:
      server:
        native:
#          search-locations: classpath:/config
          search-locations: file:///C:\Projects\schedulingtasks-app-ms\configserver\src\main\resources\config

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8888