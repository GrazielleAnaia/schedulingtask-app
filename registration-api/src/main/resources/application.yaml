spring:
#  cloud:
#    config:
#      enabled: false
  application:
    name: registration-api
  config:
    import: optional:configserver:http://localhost:8888

#logging:
#  level:
#    com.netflix.discovery: DEBUG
